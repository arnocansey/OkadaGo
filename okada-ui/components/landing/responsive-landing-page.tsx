"use client";

import { LandingPage } from "@/okada-ui/screens/okada-landing/LandingPage";

/** Single landing page for all viewports (desktop design, responsive via Tailwind). */
export function ResponsiveLandingPage() {
  return <LandingPage />;
}
